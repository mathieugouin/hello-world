version: 2

workflows:
  version: 2
  dist-compile:
    jobs:
      - compile
      - exec_program

jobs:
  compile:
    docker:
      - image: gcc:8.2
    steps:
      - checkout
      - run: cd src && make

  exec_program:
    - run: cd src && ./main
